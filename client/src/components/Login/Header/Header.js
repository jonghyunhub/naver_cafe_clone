import React from 'react';
import './Header.css';

const Header = (props)=>{
    return(
        <div id="header">
            <h1>
                <a href="#" className="naver_logo"></a>
            </h1>
        </div>
    )
}

export default Header;